C 배열 기반 단순 연결 리스트 구현 (입력/삭제/확인 기능, count는 포인터로 관리)

- 리스트는 고정 크기 배열(`Linked[SIZE]`)로 구현.
- 배열에 저장된 데이터 개수는 지역변수 `count`를 인덱스로 관리
  각 함수에 포인터(`int *count`)로 전달.
- 데이터 삽입 시, 적절한 위치를 찾아 기존 데이터를 이동시키고 삽입.
- 데이터 삭제 시, 삭제한 위치 이후 데이터를 한 칸씩 앞으로 이동.
- 배열의 최대 크기는 `#define SIZE`로 헤더에서 정의할 수 있음.
